{
  "name": "whatsapp-express-app",
  "version": "1.0.0",
  "description": "Estrutura inicial para um app Express que consome a API não oficial do WhatsApp. Configure as variáveis em `.env`, execute `npm install` e use `npm run dev` para subir o servidor de desenvolvimento. A pasta `src` já tem a estrutura modular preparada para sessões, chat e automações.",
  "type": "module",
  "main": "dist/main.js",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.4",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
