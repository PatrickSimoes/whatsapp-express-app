# WhatsApp Automations

Estrutura inicial para um app Spress que consome a API não oficial do WhatsApp. Configure as variáveis em `.env`, execute `npm install` e use `npm run dev` para subir o servidor de desenvolvimento. A pasta `src` já tem a estrutura modular preparada para sessões, chat e automações.
